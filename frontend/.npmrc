# O eslint-plugin-jsx-a11y ainda declara peer de ESLint apenas ate a v9,
# mas funciona com o ESLint 10 usado aqui. Relaxa a checagem de peer deps
# para o install (local e `npm ci` na CI) resolver sem ERESOLVE.
legacy-peer-deps=true
